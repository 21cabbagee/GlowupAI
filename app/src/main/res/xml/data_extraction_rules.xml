<?xml version="1.0" encoding="utf-8"?>
<!-- Facial images, local capture outbox files, session identity and cached metrics are sensitive
     user data. Keep them out of cloud backup and device-to-device transfer; users can use the
     explicit JSON export flow when they want a portable copy. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
    </device-transfer>
</data-extraction-rules>
