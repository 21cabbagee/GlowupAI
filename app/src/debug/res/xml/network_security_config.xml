<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only override of app/src/main/res/xml/network_security_config.xml.
  Permits cleartext (HTTP) traffic ONLY to the Android emulator's host-loopback
  alias (10.0.2.2) and localhost, so the app can talk to a local backend
  (http://10.0.2.2:8000/api/) during development. This file is never packaged
  into staging or release builds.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>
